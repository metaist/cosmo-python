# PR Build Workflow
#
# Validates changes by building all Python versions.
# Triggered on PRs that modify versions.json, scripts, or README.
#
name: PR Build

on:
  pull_request:
    paths:
      - 'versions.json'
      - 'scripts/**'

jobs:
  build:
    name: Build All Versions
    uses: ./.github/workflows/build.yaml
    with:
      python_version: "all"
